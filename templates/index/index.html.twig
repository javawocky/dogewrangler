<html lang="">
<head>

    <title>Jobs</title>
</head>
<body></body>
<h1>Hi Jim, here are your jobs</h1>

<table>
    <tr><th>JobID</th><th>Contact Name</th><th>Contact Number</th><th>Address</th><th>Status</th></tr>
    {% for job in jobs %}
        <tr><td>{{ job.id }}</td>
            <td>{{ job.clientName }}</td>
            <td>{{ job.contactNumber }}</td>
            <td>{{ job.address }}</td>
            <td>{{ job.status }}</td>
        </tr>
    {% endfor %}

</table>
</html>